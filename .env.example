# ==========================================
# VERCEL FRONTEND — set trong Vercel Dashboard
# KHÔNG commit file .env
# ==========================================

# URL nội bộ Railway (KHÔNG phải public, dùng private networking nếu có)
RAILWAY_INTERNAL_URL=https://your-service.railway.app

# Bí mật ký HMAC — phải giống INTERNAL_SECRET trên Railway
# Tạo bằng: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
INTERNAL_SECRET=f4e6d0a9f9e88a9b3f0b2f3f8c0c8f6d8a7f6e1b3c2d4e5f6a7b8c9d0e1f2a3
